#include <stdio.h> // Verifique se o símbolo '#' está aqui no seu programa também!

int main() {
    int i;

    for(i = 10; i >= 1; i = i - 1)
    {
        printf("%d\n", i);
    }
    printf("Fim");
    return 0;
}